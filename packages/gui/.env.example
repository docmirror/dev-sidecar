# Copy to `packages/gui/.env` for local builds (see gui/.gitignore).
# CI copies this file before `electron:build` when `.env` is not in the repo.
VUE_APP_PUBLISH_URL=http://dev-sidecar.docmirror.cn/update/
VUE_APP_PUBLISH_PROVIDER=generic
